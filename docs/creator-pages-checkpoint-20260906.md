# 作者与机构页面存档 · 2026-09-06

本次接续网站 main `6397c6c976cc28c66005fbab706188b97569434f`、研究仓库 main `f3f42ca9b72a1a9306982e025c60119d280a9292`。

## 已完成

- 持久 CreatorId 注册表：18,608 个实体，其中 10,721 人物、7,887 机构；67,721 条保留原始署名与职责的关联，覆盖 15,117 部已有署名的公开作品。
- `/creators` 与 `/organizations` 提供名称／别名搜索和分页；详情沿用 `/creators/c-{CreatorId}`、`/organizations/o-{CreatorId}`。
- 作品页作者、主创、机构名称跳转到对应站内页；创作者页按首次发行年份从新到旧排列并可按年份筛选，年份不明置后。仅显示规范公开 WorkId，同一作品的多个职责合并展示。
- 无人物介绍、无新增数据库。独立机构原作署名仍保留原职责，同时链接到机构实体。
- 现有外链统一为编号注释；作者、简介和评级旁显示引用号，页面底部保留链接与用途。仅明确作品商店链接标记购买／游玩，百科与资料库保留资料用途，没有新增外站链接。
- 描述资料合并采用非空字段补充；同名署名可保留多个来源。完整原始署名保存在 `originalNames`，明确多人并列署名分别显示链接。

## 数据与身份边界

新图位于 `data/public-release/v1/creators-20260906-v01/`，与研究仓库 `datasets/public-creator-graph-20260906-v01/v01/` 逐字节一致。实体注册表与关系均由 manifest 的 SHA-256、字节数和行数校验。

8,425 个实体带既有外部身份编号。无编号的署名保留其名字证据；30 条存在已知同名歧义的关系保持逐作品独立，避免把作品并入错误人物。明确附注、法定公司后缀与少量机构原作署名的规则见研究数据集 README。没有进行全面跨语种别名认证。

本次原有 35,411 源 Work、34,940 可见作品、471 个旧归并组、4,001 个源记录评级保持不变。全量比较确认原有译名／别名与短介绍不丢失，63,871 条旧署名来源保留。简介覆盖仍为 1,116 条内容概述、22,982 条识别信息；暂无可靠简介的 10,842 部继续留空。

## 验证

- `pnpm validate:release`、`pnpm audit:merge`、`pnpm validate:metadata`、`pnpm validate:creators`、`pnpm validate:ui`。
- `pnpm typecheck`、`pnpm build`、`pnpm smoke:public`（包括作者与机构正反向链接、类型不匹配 404 和注释锚点）。
- 两项描述资料与来源回归测试；研究侧七项身份／ID 持续分配测试；研究侧独立图校验。
- 用既有注册表重建，所有发布文件哈希一致。
- 已验证真实生产 HTTP 页面。云浏览器被当前环境的本地端口访问规则阻止，没有进行截图验收。

后续再生成应先用网站的 `scripts/export-public-creator-input.mjs` 导出当前目录，在研究侧传入既有注册表 `--previous` 构建新版本，然后同步网站图快照。禁止通过全量重排重新分配 CreatorId，也不能让新署名字段影响作品原有的去重或评级。
