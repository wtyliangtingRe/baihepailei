# Source import fixtures

Fixtures in this directory are tiny, repository-safe samples used by tests and local adapter checks.

They must not contain:

- full source dumps
- user comments or reviews
- private notes
- large scraped datasets

Real fetched data belongs under local-only `data_local/` and must not be committed.
