import CollectionIndexPage from '../_components/CollectionIndexPage'

export default function TermsIndexPage() {
  return <CollectionIndexPage collection="terms" description="Term entries from the Lite search index." eyebrow="terms" title="Terms" />
}
